version: "3.9"

services:
  yt-dlp-ui:
    build: .
    ports:
      - "8081:8080"
    environment:
      BIND_ADDR: "0.0.0.0:8080"
      DATA_DIR: /data
      DOWNLOADS_DIR: /data/downloads
      ARCHIVES_DIR: /data/archives
    volumes:
      - ./data:/data
    restart: unless-stopped
